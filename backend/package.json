{
  "name": "schedula-backend",
  "version": "1.0.0",
  "description": "JSON Server backend for Schedula app",
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "json-server --watch ../mock/db.json --port 3001 --host 0.0.0.0"
  },
  "dependencies": {
    "json-server": "^1.0.0-beta.3",
    "cors": "^2.8.5"
  },
  "engines": {
    "node": ">=18.0.0"
  }
} 